title: Self-Query Retrieval: A Practical Guide
intro: Half the sentence is a subject. Half of it is a WHERE clause.

## The idea in brief

Similarity search compares meaning, and that is all it does. Ask it for "papers about attention published after 2020" and the words <em>published after 2020</em> are treated as more subject matter to match, not as a rule to enforce. A 2017 paper that is squarely about attention will score well and be returned, because nothing in a cosine score knows what a year is.

## Two questions in one sentence

A self-query retriever hands the sentence to an LLM along with a description of the metadata each document carries, and asks it to return two things: the <strong>semantic query</strong> &mdash; the part that should be embedded and compared &mdash; and a <strong>structured filter</strong> over the metadata, in the form of real comparisons like <code>year &gt; 2020</code>. The filter runs first as an ordinary database-style predicate; similarity then ranks only the documents that survived it. The constraint becomes a guarantee rather than a hint.

## Turning a question into a filter plus a search

Many questions contain two different kinds of requirement, and a plain vector search can only handle one of them.

<blockquote>"What did the 2023 security reports say about phishing?"</blockquote>

"Phishing" is a <strong>semantic</strong> requirement &mdash; find passages about it. "2023" and "security reports" are <strong>structured</strong> requirements &mdash; filter on a date field and a document-type field.

Embedding the whole question and searching gives poor results: the year and the document type become part of a blended vector, so documents from 2019 with the word "2023" in the body may rank above the right ones.

A self-query retriever uses a language model to split the question:

<p class="mono-font">"What did the 2023 security reports say about phishing?"</p>
<p class="mono-font">&rarr; query: "phishing" &nbsp;+&nbsp; filter: year = 2023 AND type = "security_report"</p>

Then it runs a filtered vector search: the filter constrains the candidate set, and the semantic query orders what remains.

## What it needs to work

Two things, and the first is the part that is easy to under-invest in.

<strong>Metadata on every document, populated at indexing time.</strong> The filter can only reference fields that exist. Adding a date field later means re-indexing.

<strong>A schema description given to the model,</strong> so it knows which fields exist and what values they take:

```python
metadata_fields = [
    {"name": "year", "type": "integer", "description": "Publication year"},
    {"name": "doc_type", "type": "string",
     "description": "One of: policy, report, guide, minutes"},
    {"name": "department", "type": "string",
     "description": "Owning department, e.g. Security, HR, Finance"},
]
```

The model is prompted with that schema and the user's question, and asked to return a structured object with a query string and a filter expression. Constraining the output to a JSON schema is worth doing &mdash; it converts a class of malformed-filter failures into a validation error.

## The failure modes

This is a language model producing a filter, so it fails in language-model ways, and each failure needs a specific guard.

<strong>Invented field names.</strong> The model filters on `publication_date` when the schema says `year`. Validate the filter against the schema and drop or repair unknown fields.

<strong>Invented values.</strong> A filter for `doc_type = "reports"` when the actual value is `"report"`. Enumerate allowed values in the schema, and fuzzy-match or reject.

<strong>Over-filtering.</strong> The model adds a constraint the user did not intend, and the result set is empty. The standard remedy is to <strong>fall back to an unfiltered search</strong> when the filtered one returns nothing, and to say so in the response.

<strong>Under-filtering.</strong> The model ignores a constraint the user did state, and irrelevant results come back. Harder to detect automatically; it shows up in evaluation.

<strong>Latency.</strong> An extra model call before retrieval begins, adding several hundred milliseconds. Cache the parse for repeated queries.

The practical shape that results:

```python
parsed = parse_query(question, schema)          # LLM call
parsed.filter = validate(parsed.filter, schema) # drop unknown fields/values

results = index.search(parsed.query, filter=parsed.filter, k=10)
if not results:
    results = index.search(parsed.query, k=10)  # graceful degradation
```

## Exploration guide

1. <strong>Read the parse for the first query.</strong> Note what was removed from the semantic half: only <em>attention</em> is left to match on, because the date has become a filter instead.
2. <strong>Turn the filter off.</strong> This is plain similarity search over everything. A 2017 paper takes the top spot &mdash; it is genuinely the best match for "attention", and it breaks the constraint the reader stated.
3. <strong>Turn it back on.</strong> The 2017 paper is struck out with the reason it failed, and the top result is now both about attention and inside the requested date range.
4. <strong>Try "short surveys on retrieval".</strong> Two constraints this time, on different fields and different types &mdash; a number and a category &mdash; combined with AND.
5. <strong>Try "anything about attention".</strong> No constraint in the sentence, so the filter is empty and the retriever degrades gracefully into ordinary similarity search.

## The short of it

Embeddings compare meaning and cannot enforce a rule, so any constraint left inside the query text is at best a weak hint. Self-querying separates the sentence into the part worth embedding and the part worth executing as a filter, which turns "after 2020" from a phrase competing for cosine similarity into a predicate that simply removes the documents that fail it. The trade is an extra LLM call before retrieval, and a hard dependency on documents actually carrying the metadata the filter names.

## Where it earns its place

<strong>Time-bounded questions.</strong> "Last quarter", "since the policy changed", "in 2023". Dates are exactly the kind of constraint embeddings handle badly and filters handle perfectly.

<strong>Faceted corpora.</strong> Documents with meaningful categories &mdash; department, product line, region, document type, author. Users naturally phrase questions that reference them.

<strong>Numeric constraints.</strong> "Products under &pound;50", "incidents affecting more than 100 users". A vector search cannot express an inequality.

<strong>Mixed catalogues.</strong> E-commerce is the canonical case: "waterproof jacket under &pound;80 in medium" is one semantic term and three filters.

Where it is unnecessary: a homogeneous corpus with no meaningful metadata, or an interface where the user already selects filters in the UI. If the filters are available as controls, use the controls &mdash; they are free and cannot be hallucinated.

## Related retrieval strategies

|Strategy|What it adds|
|---|---|
|Self-query|Extracts filters from the question|
|Multi-query|Rewrites the question several ways and unions the results|
|HyDE|Generates a hypothetical answer and searches with that|
|Parent document|Retrieves small, returns large|
|Reranking|Reorders candidates with a cross-encoder|

These compose. A realistic pipeline: self-query to extract filters, multi-query to broaden the semantic search, hybrid retrieval over the filtered set, reciprocal rank fusion, then a cross-encoder rerank.

Each stage adds latency, so the order to add them in is by measured benefit. In most systems, hybrid search and reranking are the largest gains; self-query matters specifically when the corpus has metadata users refer to.

## Splitting a sentence into a filter and a search

A self-query retriever reads one sentence and produces two things -- a metadata filter and a semantic query. This runs both halves over a small collection, and shows what happens at each of the failure modes the article names.

```python-run
import numpy as np

DOCS = [
    # (text-axis vector, metadata)
    ([0.9, 0.1, 0.0], {"year": 2024, "author": "Chen", "type": "paper",
                       "title": "Retrieval augmentation at scale"}),
    ([0.85, 0.15, 0.0], {"year": 2021, "author": "Chen", "type": "paper",
                         "title": "Early retrieval methods"}),
    ([0.8, 0.2, 0.0], {"year": 2024, "author": "Patel", "type": "blog",
                       "title": "RAG in production"}),
    ([0.1, 0.9, 0.0], {"year": 2024, "author": "Chen", "type": "paper",
                       "title": "Image segmentation advances"}),
    ([0.15, 0.85, 0.0], {"year": 2019, "author": "Ito", "type": "paper",
                         "title": "Convolutional architectures"}),
    ([0.05, 0.05, 0.95], {"year": 2023, "author": "Ito", "type": "blog",
                          "title": "Notes on databases"}),
]
AXES = ["retrieval", "vision", "databases"]

def unit(v):
    v = np.asarray(v, float)
    return v / np.linalg.norm(v)

QUESTION = "papers about retrieval from 2024"
print("THE QUESTION: %r" % QUESTION)
print("it contains two different kinds of constraint, and they need")
print("completely different machinery:")
print("%-24s %-30s %s" % ("part", "what it is", "handled by"))
print("%-24s %-30s %s" % ("'about retrieval'", "a semantic notion",
                          "vector similarity"))
print("%-24s %-30s %s" % ("'papers'", "an exact field value",
                          "a metadata filter"))
print("%-24s %-30s %s" % ("'from 2024'", "an exact field value",
                          "a metadata filter"))
print()

# what the LLM extracts
QUERY_VEC = [0.9, 0.1, 0.0]
FILTER = {"type": "paper", "year": 2024}
print("AN LLM PARSES IT INTO:")
print("      semantic query: 'retrieval'  ->  %s" % str(QUERY_VEC))
print("      filter:         %s" % str(FILTER))
print()

def search(qvec, flt, k=3):
    out = []
    for v, m in DOCS:
        if flt and any(m.get(f) != val for f, val in flt.items()):
            continue
        out.append((float(unit(qvec) @ unit(v)), m))
    return sorted(out, key=lambda x: -x[0])[:k]

print("SEARCH WITHOUT THE FILTER -- similarity alone:")
print("%-40s %8s %8s %10s %10s" % ("title", "year", "type", "author", "sim"))
for s, m in search(QUERY_VEC, None, k=6):
    print("%-40s %8d %8s %10s %10.4f"
          % (m["title"][:38], m["year"], m["type"], m["author"], s))
print("   the top hits are all about retrieval, correctly. but the 2021")
print("   paper and the 2024 BLOG are in there too, and the question")
print("   excluded both. no similarity threshold fixes this: 'from 2024'")
print("   is not a direction in the embedding space, it is a fact about a")
print("   field.")
print()

print("SEARCH WITH THE FILTER APPLIED FIRST:")
print("%-40s %8s %8s %10s %10s" % ("title", "year", "type", "author", "sim"))
for s, m in search(QUERY_VEC, FILTER):
    print("%-40s %8d %8s %10s %10.4f"
          % (m["title"][:38], m["year"], m["type"], m["author"], s))
kept = [m for v, m in DOCS
        if all(m.get(f) == val for f, val in FILTER.items())]
print("   note the second row: a vision paper, at similarity %.4f, is now"
      % search(QUERY_VEC, FILTER)[-1][0])
print("   in the results. only %d documents passed the filter, so the"
      % len(kept))
print("   search had nothing better to offer -- filtering constrains WHAT")
print("   can be returned, and never guarantees that what remains is any")
print("   good. a relevance floor is a separate decision.")
print("   the filter reduced the collection from %d documents to %d before"
      % (len(DOCS), len(kept)))
print("   any similarity was computed. that ORDER matters -- filtering")
print("   first means the vector search only ever ranks eligible")
print("   documents, and it can never be forced to choose between")
print("   relevance and correctness.")
print()

print("FAILURE MODE 1 -- A FILTER THE SCHEMA CANNOT EXPRESS. suppose the")
print("model extracts a field that does not exist:")
for bad in ({"venue": "NeurIPS"}, {"year": 2025}, {"type": "Paper"}):
    res = search(QUERY_VEC, bad)
    print("      filter %-24s -> %d results"
          % (str(bad), len(res)))
print("   all three return NOTHING, and for three different reasons:")
print("      'venue' is not a field in this collection at all.")
print("      2025 is a valid field with no matching documents.")
print("      'Paper' is the right field and the wrong CASE.")
print("   the third is the cruel one. the user asked a reasonable")
print("   question, the parse looks correct, and an exact-match filter")
print("   returns an empty set with no error anywhere. that is why the")
print("   schema handed to the LLM must list the allowed VALUES, not just")
print("   the field names.")
print()

print("FAILURE MODE 2 -- SOMETHING FILTERABLE LEFT IN THE SEMANTIC QUERY.")
print("if the parser misses 'from 2024' and leaves it in the text:")
MISSED = [0.85, 0.10, 0.0]        # 'retrieval from 2024' as a vector
print("%-40s %8s %10s" % ("title", "year", "sim"))
for s, m in search(MISSED, None, k=4):
    print("%-40s %8d %10.4f" % (m["title"][:38], m["year"], s))
print("   the years in the results are whatever the similarity happened to")
print("   rank, because '2024' contributes almost nothing to a sentence")
print("   embedding. a date is a token like any other, and a model has no")
print("   reason to encode it as a constraint.")
print("   this is the failure the whole pattern exists to prevent, and it")
print("   is invisible: the results look plausible and quietly violate the")
print("   question.")
print()

print("FAILURE MODE 3 -- OVER-FILTERING. a filter too narrow leaves")
print("nothing to rank:")
print("%-52s %14s %10s" % ("filter", "eligible docs", "results"))
for f in ({}, {"type": "paper"}, {"type": "paper", "year": 2024},
          {"type": "paper", "year": 2024, "author": "Patel"}):
    elig = [m for v, m in DOCS
            if all(m.get(k2) == val for k2, val in f.items())]
    print("%-52s %14d %10d"
          % (str(f) if f else "(none)", len(elig), len(search(QUERY_VEC, f))))
print("   every clause the model adds multiplies the risk of an empty")
print("   result. the standard mitigations are to allow the filter to be")
print("   relaxed and retried, or to give the LLM a schema with value")
print("   counts so it can see which clauses are safe.")
print()

print("WHAT IT NEEDS TO WORK, and none of these are optional:")
print("%-34s %s" % ("metadata actually stored", "filters read fields"))
print("%-34s %s" % ("a schema given to the LLM", "field names AND values"))
print("%-34s %s" % ("a vector store with filtering", "not all support it"))
print("%-34s %s" % ("filter-then-search, not after", "post-filtering breaks k"))
print("   that last row is worth spelling out. if the store retrieves the")
print("   top k by similarity and THEN filters, you asked for %d and can"
      % 3)
print("   receive anywhere from 0 to %d:" % 3)
top_all = search(QUERY_VEC, None, k=3)
after = [m for s, m in top_all
         if all(m.get(f) == val for f, val in FILTER.items())]
n1, n2 = len(after), len(search(QUERY_VEC, FILTER))
print("      top 3 by similarity, then filtered: %d result%s"
      % (n1, "" if n1 == 1 else "s"))
print("      filtered, then top 3:               %d result%s"
      % (n2, "" if n2 == 1 else "s"))
print("   same data, same filter, different answers -- and the first one")
print("   silently returns fewer results as the filter gets stricter.")
```

## Questions people ask

<strong>Does it need a large model?</strong> No &mdash; extracting a filter from a question is a small structured task, and a fast, cheap model handles it well. Use structured output constraints.

<strong>What if the model produces an invalid filter?</strong> Validate against the schema and fall back to an unfiltered search. Never pass an unvalidated filter to the store.

<strong>How much latency does it add?</strong> One model call, typically 200&ndash;600ms. Cache by query text.

<strong>Can I use it without a vector database?</strong> The filtering needs a store that supports metadata predicates. Most do; plain in-memory vector arrays generally do not.

<strong>Should filters be hard or soft?</strong> Hard for correctness constraints such as permissions and dates. Consider soft (a score boost) for preferences, where over-filtering to zero results is worse than imperfect ordering.

<strong>Does it replace hybrid search?</strong> No &mdash; it constrains the candidate set. Hybrid search still decides the ordering within it.

## Recap in one screen

- Questions often mix a semantic requirement with structured constraints; embeddings handle only the first.
- A model parses the question into a query string plus a metadata filter, which is then applied at search time.
- It requires metadata populated at indexing time and a schema description in the prompt.
- Validate the generated filter against the schema, and fall back to unfiltered search when it returns nothing.
- Most valuable for dates, numeric constraints and faceted corpora; unnecessary when the UI already offers filters.
